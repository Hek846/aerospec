# Postgres
POSTGRES_PASSWORD=aerospec

# API
DATABASE_URL=postgres://aerospec:aerospec@localhost:5432/aerospec
JWT_SECRET=change-me-to-a-long-random-string
OPENAQ_API_KEY=
PORT=4000
FRONTEND_URL=http://localhost:8080
# Dev only: extend every device's readings + last_seen every 2 min so seeded
# demo data keeps moving and devices stay "online". Leave unset in production.
SIMULATE_LIVE=false

# Web (build-time)
VITE_API_URL=http://localhost:4000
